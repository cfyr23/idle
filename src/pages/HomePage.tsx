import { FC, useEffect, useState } from 'react'import coinImage from '../assets/coin.svg'import energyImage from '../assets/energy-icon.svg'import CanvasClick from '../components/CanvasClick';interface IHomeProps{    balance: number,    setBalance: (num :number) => void,    clickValue: number,    totalClicked: number,    setTotalClicked: (num: number) => void,}const HomePage: FC<IHomeProps> = ({balance, setBalance, clickValue, totalClicked, setTotalClicked}) => {    const [position, setPosition] = useState({ x: 0, y: 0 });    const [isCanvasShowed, setIsCanvasShowed] = useState<boolean>(false);    const [isImageClicked, setIsImageClicked] = useState<boolean>(false);    const handleImageClick = () => {        setIsImageClicked(true);        setIsCanvasShowed(true);    }    const handleMouseMove = (e: React.MouseEvent<HTMLDivElement>) => {        setPosition({ x: e.clientX, y: e.clientY });    };    useEffect(() => {        setTimeout(() => {            setIsImageClicked(false)        }, 50)    }, [isImageClicked])    useEffect(() => {        setTimeout(() => {            setIsCanvasShowed(false)        }, 50)    }, [isCanvasShowed])    return (        <div className="page__content home">            <div className="home__top">                <div className="stat">                    <div className="info__balance">                        <img src={coinImage} alt="coin"/>                        <p>{balance}</p>                    </div>                    <div className="info__energy">                        <img src={energyImage} alt="energy"/>                        <p>100/1000</p>                    </div>                </div>                <div className="home__booster">                    <div className="booster__item">                        <p className="booster__item-title">Автоклик</p>                        <p className="booster__item-description">Сборщик<br/>злата</p>                        <button className="btn booster__item-btn"><img src={coinImage} alt="coin"/>10K</button>                    </div>                    <div className="booster__item">                        <p className="booster__item-title">Злато</p>                        <p className="booster__item-description">+1 злата<br/>за клик</p>                        <button className="btn booster__item-btn"><img src={coinImage} alt="coin"/>100</button>                    </div>                    <div className="booster__item">                        <p className="booster__item-title">Удаль</p>                        <p className="booster__item-description">+250 запас<br/>удали</p>                        <button className="btn booster__item-btn"><img src={coinImage} alt="coin"/>100</button>                    </div>                </div>            </div>            <div                className={isImageClicked ? "home__coin clicked" : "home__coin"}                onClick={(e) => {                    handleImageClick()                    setBalance(balance + clickValue)                    setTotalClicked(totalClicked + clickValue)                    handleMouseMove(e)                }}            >                <img src={coinImage} className="home__bigcoin" alt="bigcoin"/>            </div>            {/* canvas for click value */}            {position.x && position.y && isCanvasShowed ?                <CanvasClick x={position.x} y={position.y} click={clickValue}/> : null}        </div>    )}export default HomePage